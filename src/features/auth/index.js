/**
 * FEATURE: Authentication
 * Responsibility: Login, signup, session management, password reset
 * 
 * FILES:
 * - auth-manager.js - Core auth logic  
 * - auth.js - UI bindings and forms
 * - README.md - Feature documentation
 * 
 * PUBLIC API:
 */

// Login/signup - handle auth flows
// export { login, signup, logout, resetPassword } from './auth-manager.js';

// Session management
// export { checkAuthStatus, getCurrentUser, refreshToken } from './auth-manager.js';

/**
 * USAGE:
 * import { login, logout } from './auth/index.js';
 * 
 * const result = await login(email, password);
 * if (result.success) {
 *     // Redirect to dashboard
 * }
 */
