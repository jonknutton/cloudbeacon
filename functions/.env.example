# Cloud Functions Environment Variables
# 
# These variables control the email sending functionality.
# Set them in the Firebase Console:
# 1. Go to Firebase Console → Cloud Functions
# 2. Click "sendAccountSecurityEmail" function
# 3. Click "Runtime settings" tab
# 4. Add these environment variables under "Runtime environment variables"
#
# OR deploy with: firebase deploy --only functions
#    (after setting these in your system environment)

# Email server configuration (Gmail or SendGrid)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# Email sender
SMTP_FROM=security@cloudbeacon.app

# Your app's public URL (used in unlock email links)
APP_URL=https://cloudbeacon-e61f6.web.app
